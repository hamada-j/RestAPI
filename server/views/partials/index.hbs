<div>
    {{#each prod}}
    <div class="row">
        <div class="col-md-12">
            <h3>{{_id}}</h3>
            <h3>{{name}}</h3>
            <h3>{{productImage}}</h3>
            <div class="img">
                <img src="{{productImage}}" alt="">
            </div>
        </div>
    </div>
    {{/each}}
</div>